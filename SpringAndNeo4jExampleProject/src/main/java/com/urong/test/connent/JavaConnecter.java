package com.urong.test.connent;

public class JavaConnecter {

/*	public static void main(String[] args) {

		Config noSSL = Config.build().withEncryptionLevel(Config.EncryptionLevel.NONE).toConfig();
		Driver driver = GraphDatabase.driver("bolt://107.23.94.190:33947",
				AuthTokens.basic("neo4j", "discard-rig-rheostat"), noSSL); // <password>
		try (Session session = driver.session()) {
			String cypherQuery = "MATCH (n) " + "RETURN id(n) AS id";
			StatementResult result = session.run(cypherQuery, parameters());
			while (result.hasNext()) {
				System.out.println(result.next().get("id"));
			}
		}

	}*/
}
